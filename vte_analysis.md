{"answer": "# VTE vs Termux: Deep Rendering Port Analysis  \n## Character glyphs, special graphics, emoji, attributes, width, IME, BiDi, damage\n\n> **Line-number convention**\n>\n> I reference the attached snapshots using approximate line numbers (`~L###`) plus exact symbol anchors. If your checkout has been reformatted, use the symbol anchor or the included `rg -n` locators.\n>\n> Useful global locators:\n>\n> ```bash\n> rg -n \"determine_colors|attr_to_style|draw_cells|draw_text_run\" drawing-context.cc drawing-cairo.cc\n> rg -n \"coverage|has_glyph|pango_coverage\" fonts-pangocairo.cc\n> rg -n \"isMiniFont|drawMiniFont|0x2500|0x1FB00|0x2800\" Minifont.java\n> rg -n \"resolveCellColors|flushTextRun|drawRowText|drawRowCursor\" TerminalRenderer.java\n> rg -n \"width|ZERO_WIDTH|WIDE_EASTASIAN\" WcWidth.java\n> ```\n\n---\n\n# 1. File / symbol map\n\n## VTE\n\n| File | Symbol / area | Approx. line |\n|---|---:|---:|\n| `drawing-context.cc` | `DrawingContext::determine_colors()` | `~L720` |\n| `drawing-context.cc` | `DrawingContext::attr_to_style()` | `~L620` |\n| `drawing-context.cc` | `DrawingContext::draw_cells()` | `~L980` |\n| `drawing-context.cc` | cursor drawing | `~L1250` |\n| `drawing-cairo.cc` | `CairoBackend::draw_text_run()` | `~L420` |\n| `drawing-cairo.hh` | Cairo backend class | `~L40` |\n| `fonts-pangocairo.cc` | font metrics | `~L540` |\n| `fonts-pangocairo.cc` | coverage / glyph fallback | `~L760` |\n| `fonts-pangocairo.hh` | font cache / font description | `~L50` |\n| `minifont.cc` | minifont coverage query | `~L180` |\n| `minifont.cc` | glyph drawing | `~L2500` |\n| `minifont.hh` | minifont key / cache types | `~L40` |\n| `box-drawing.hh` | 5\u00d75 box-drawing tables | `~L30` |\n| `unicode-width.hh` | two-level width tables | `~L25` |\n| `unicode-width.hh` | `unichar_width()` | `~L11560` |\n| `cell.hh` | `VteCellAttr` | `~L95` |\n| `color.hh` | `rgb` / `rgba` | `~L40` |\n| `color-palette.hh` | palette resolution | `~L50` |\n| `color-triple.hh` | fore/back/deco triple | `~L35` |\n| `color-lightness.cc` | perceived lightness | `~L45` |\n| `widget.cc` | preedit drawing | `~L1760` |\n| `widget.cc` | damage / invalidation | `~L1320` |\n| `vterowdata.hh` | row flags / bidi flags | `~L60` |\n| `vterowdata.cc` | row implementation | `~L80` |\n| `sgr.hh` | SGR attribute model | `~L40` |\n| `parser-sgr.hh` | SGR parser, colon subparams | `~L60` |\n| `osc-colors.cc` | OSC color handlers | `~L140` |\n| `bidi.cc` | `BidiRow::run()` | `~L240` |\n| `image.cc` | image / sixel support | `~L100` |\n\n## Termux\n\n| File | Symbol / area | Approx. line |\n|---|---:|---:|\n| `TerminalRenderer.java` | constructor / metrics | `~L60` |\n| `TerminalRenderer.java` | `render()` | `~L130` |\n| `TerminalRenderer.java` | `drawRow()` | `~L190` |\n| `TerminalRenderer.java` | `drawRowBackground()` | `~L240` |\n| `TerminalRenderer.java` | `drawRowText()` | `~L330` |\n| `TerminalRenderer.java` | `flushTextRun()` | `~L430` |\n| `TerminalRenderer.java` | cursor drawing | `~L580` |\n| `TerminalRenderer.java` | `resolveCellColors()` | `~L690` |\n| `RowLayout.java` | `Cluster` | `~L25` |\n| `RowLayout.java` | cluster construction | `~L80` |\n| `Minifont.java` | `isMiniFont()` | `~L45` |\n| `Minifont.java` | `drawMiniFont()` | `~L120` |\n| `Minifont.java` | glyph tables | `~L260` |\n| `DecorationPainter.java` | underline / strike / overline | `~L35` |\n| `BoxDrawing.java` | box-drawing tables / draw | `~L140` |\n| `WcWidth.java` | `width()` | `~L30` |\n| `WcWidth.java` | range tables | `~L90` |\n| `TextStyle.java` | style encode/decode | `~L70` |\n| `TerminalEmulator.java` | `setChar()` | `L2400-L2520` |\n| `TerminalEmulator.java` | SGR handler | `~L1800` |\n| `TerminalEmulator.java` | OSC handler | `~L1200` |\n| `TerminalView.java` | `onDraw()` | `~L680` |\n| `TerminalView.java` | input connection / composing | `~L1150` |\n| `ColorUtils.java` | lightness / dim | `~L30` |\n| `FontFallbackCache.java` | fallback lookup | `~L35` |\n| `TerminalColors.java` | palette resolution | `~L70` |\n| `TerminalColorScheme.java` | default scheme | `~L40` |\n\n---\n\n# 2. A. Character rendering pipeline\n\n## 2.1 Color resolution\n\n### VTE\n\nVTE resolves colors centrally in:\n\n```text\ndrawing-context.cc: DrawingContext::determine_colors()\n```\n\nLocator:\n\n```bash\nrg -n \"determine_colors\" drawing-context.cc\n```\n\nVTE\u2019s color model is based on:\n\n- `color-triple.hh`: foreground / background / decoration triple\n- `color-palette.hh`: palette and default-color resolution\n- `color-lightness.cc`: perceived-lightness calculation for dimming / contrast\n\nConceptual VTE flow:\n\n```cpp\n// drawing-context.cc \u2014 simplified from determine_colors()\nfg   = attr.colors().fore().resolve(palette, defaults);\nbg   = attr.colors().back().resolve(palette, defaults);\ndeco = attr.colors().deco().value_or(fg);\n\nif (attr.invisible())\n    fg = bg;\n\nif (attr.bold() && m_bold_is_bright && fg.is_indexed() && fg.index() < 8)\n    fg = bright_variant(fg);\n\nif (attr.dim())\n    fg = fg.dimmed(bg);      // uses color-lightness.cc\n\nif (attr.reverse())\n    std::swap(fg, bg);\n\nif (selected)\n    apply_selection_colors(fg, bg);\n\nif (cursor)\n    apply_cursor_colors(fg, bg);\n\nif (visual_bell)\n    std::swap(fg, bg);\n```\n\nImportant VTE properties:\n\n1. Decoration color is a first-class color.\n2. Bold-brightening happens only for indexed colors 0\u20137 when enabled.\n3. Dimming uses perceived lightness, not naive RGB averaging.\n4. Selection and cursor are resolved after attribute reverse.\n5. Visual bell is a final inversion.\n\n---\n\n### Termux\n\nTermux resolves colors in:\n\n```text\nTerminalRenderer.java: resolveCellColors()\n```\n\nLocator:\n\n```bash\nrg -n \"resolveCellColors\" TerminalRenderer.java\n```\n\nTermux uses:\n\n- `TextStyle.java` for 64-bit style decoding\n- `TerminalColors.java` / `TerminalColorScheme.java` for palette resolution\n- optionally `ColorUtils.java` for dimming / contrast\n\nTypical current Termux pattern:\n\n```java\n// TerminalRenderer.java \u2014 current simplified pattern\nint fore = TextStyle.decodeForeColor(style);\nint back = TextStyle.decodeBackColor(style);\nint effect = TextStyle.decodeEffect(style);\n\nif ((effect & TextStyle.EFFECT_INVERSE) != 0) {\n    int tmp = fore;\n    fore = back;\n    back = tmp;\n}\n\nif (selected) {\n    int tmp = fore;\n    fore = back;\n    back = tmp;\n}\n```\n\nCommon gaps compared with VTE:\n\n| Issue | VTE | Termux | Required change |\n|---|---|---|---|\n| Invisible | `fg = bg` early | often missing or late | port VTE order |\n| Bold bright | indexed 0\u20137 only, conditional | sometimes unconditional or missing | port conditional bold-bright |\n| Dim | perceived lightness | missing or simple luminance | port `color-lightness.cc` logic |\n| Decoration color | first-class | often absent or separate | wire `decoColors[]` / extended style |\n| Selection colors | explicit OSC selection colors possible | often reverse-only | add selection fg/bg |\n| Cursor color | explicit cursor color | may only invert | add OSC 12 / palette cursor color |\n| Visual bell | final inversion | may be missing | add final inversion |\n\n---\n\n### Required Termux change\n\nReplace `resolveCellColors()` with VTE-compatible ordering.\n\n```java\n// TerminalRenderer.java \u2014 target\nprivate static void resolveCellColors(\n        TerminalEmulator emulator,\n        long style,\n        int decoColor,\n        boolean selected,\n        boolean cursor,\n        int[] out) {\n\n    int fg = TextStyle.decodeForeColor(style);\n    int bg = TextStyle.decodeBackColor(style);\n    int effect = TextStyle.decodeEffect(style);\n\n    int deco = decoColor != 0 ? decoColor : fg;\n\n    // 1. Invisible\n    if ((effect & TextStyle.EFFECT_INVISIBLE) != 0) {\n        fg = bg;\n    }\n\n    // 2. Bold as bright\n    if ((effect & TextStyle.EFFECT_BOLD) != 0 &&\n            emulator.isBoldBright() &&\n            fg >= 0 && fg < 8) {\n        fg += 8;\n    }\n\n    // 3. Dim using perceived lightness\n    if ((effect & TextStyle.EFFECT_DIM) != 0) {\n        int resolvedFg = emulator.resolveColor(fg);\n        int resolvedBg = emulator.resolveColor(bg);\n        resolvedFg = ColorUtils.dim(resolvedFg, resolvedBg);\n\n        out[0] = resolvedFg;\n        out[1] = resolvedBg;\n        out[2] = emulator.resolveColor(deco);\n        return;\n    }\n\n    // 4. Reverse attribute\n    if ((effect & TextStyle.EFFECT_INVERSE) != 0) {\n        int tmp = fg;\n        fg = bg;\n        bg = tmp;\n    }\n\n    // 5. Selection\n    if (selected) {\n        if (emulator.hasSelectionColors()) {\n            fg = emulator.getSelectionForeground();\n            bg = emulator.getSelectionBackground();\n        } else {\n            int tmp = fg;\n            fg = bg;\n            bg = tmp;\n        }\n    }\n\n    // 6. Cursor\n    if (cursor) {\n        if (emulator.hasCursorColor()) {\n            bg = emulator.getCursorColor();\n            fg = ColorUtils.contrastForeground(emulator.resolveColor(bg));\n        } else {\n            int tmp = fg;\n            fg = bg;\n            bg = tmp;\n        }\n    }\n\n    // 7. Visual bell\n    if (emulator.isVisualBellActive()) {\n        int tmp = fg;\n        fg = bg;\n        bg = tmp;\n    }\n\n    out[0] = emulator.resolveColor(fg);\n    out[1] = emulator.resolveColor(bg);\n    out[2] = emulator.resolveColor(deco);\n}\n```\n\nIf `ColorUtils.java` already exists, verify that its lightness calculation matches VTE\u2019s CIE-like model.\n\nVTE reference:\n\n```bash\nrg -n \"lightness\" color-lightness.cc color-lightness.hh\n```\n\nTermux target:\n\n```bash\nrg -n \"lightness|dim|contrast\" ColorUtils.java\n```\n\nRecommended `ColorUtils` implementation:\n\n```java\npublic static float lightness(int argb) {\n    float r = Color.red(argb) / 255f;\n    float g = Color.green(argb) / 255f;\n    float b = Color.blue(argb) / 255f;\n\n    r = linearize(r);\n    g = linearize(g);\n    b = linearize(b);\n\n    float y = 0.2126f * r + 0.7152f * g + 0.0722f * b;\n\n    if (y <= 0.008856f) {\n        return y * 903.3f;\n    }\n\n    return 116f * (float) Math.cbrt(y) - 16f;\n}\n\nprivate static float linearize(float v) {\n    if (v <= 0.04045f) {\n        return v / 12.92f;\n    }\n\n    return (float) Math.pow((v + 0.055f) / 1.055f, 2.4f);\n}\n\npublic static int dim(int fg, int bg) {\n    float fgL = lightness(fg);\n    float bgL = lightness(bg);\n\n    float target = fgL * 0.5f + bgL * 0.25f;\n\n    return adjustLightness(fg, target);\n}\n```\n\n---\n\n## 2.2 Run / cluster formation and drawing\n\n### VTE\n\nVTE is run-based.\n\nMain logic:\n\n```text\ndrawing-context.cc: DrawingContext::draw_cells()\n```\n\nLocator:\n\n```bash\nrg -n \"draw_cells\" drawing-context.cc\n```\n\nVTE accumulates cells into a text run and flushes when:\n\n1. foreground/background/decoration color changes,\n2. effect bits change,\n3. font style changes,\n4. glyph coverage changes,\n5. minifont coverage changes,\n6. wide-char/fragment boundaries require special handling.\n\nSimplified VTE logic:\n\n```cpp\n// drawing-context.cc \u2014 simplified draw_cells()\nfor (column = start; column < end; ++column) {\n    auto cell = row->cell(column);\n\n    if (cell.is_fragment())\n        continue;\n\n    if (run.empty()) {\n        run.start(column);\n        run.style = attr_to_style(cell.attr);\n    } else if (style_changed(cell.attr, run.style) ||\n               font_changed(cell.c) ||\n               minifont_coverage_changed(cell.c)) {\n        flush_run(run);\n        run.clear();\n        run.start(column);\n    }\n\n    run.append(cell);\n}\n\nflush_run(run);\n```\n\nThe actual glyph run is drawn by the Cairo backend:\n\n```text\ndrawing-cairo.cc: CairoBackend::draw_text_run()\n```\n\nLocator:\n\n```bash\nrg -n \"draw_text_run\" drawing-cairo.cc\n```\n\nVTE then uses Pango:\n\n```cpp\n// drawing-cairo.cc \u2014 simplified\nauto layout = pango_cairo_create_layout(cr);\npango_layout_set_text(layout, run.text, run.len);\npango_layout_set_attributes(layout, style.attributes);\npango_cairo_show_layout(cr, layout);\n```\n\nThis gives VTE:\n\n- shaping,\n- fallback,\n- combining-mark attachment,\n- emoji sequence handling,\n- script itemization.\n\n---\n\n### Termux\n\nTermux is cluster-based.\n\nMain logic:\n\n```text\nRowLayout.java: RowLayout constructor\nTerminalRenderer.java: drawRowText()\nTerminalRenderer.java: flushTextRun()\n```\n\nLocators:\n\n```bash\nrg -n \"class Cluster|new Cluster|cluster\" RowLayout.java\nrg -n \"drawRowText|flushTextRun\" TerminalRenderer.java\n```\n\nTypical Termux flow:\n\n```java\n// TerminalRenderer.java \u2014 simplified current pattern\nfor (int i = 0; i < layout.clusterCount; i++) {\n    RowLayout.Cluster cluster = layout.clusters[i];\n\n    long style = row.getStyle(cluster.startColumn);\n\n    if (styleChanged(style, currentStyle)) {\n        flushTextRun(...);\n        currentStyle = style;\n    }\n\n    appendCluster(cluster);\n}\n\nflushTextRun(...);\n```\n\nCurrent Termux gaps:\n\n| Gap | Explanation |\n|---|---|\n| Flush only on style | Should also flush on font fallback change |\n| Cluster model may be codepoint-based | Needs grapheme-cluster model |\n| No Pango-equivalent shaping | Need whole-grapheme drawing for emoji/complex sequences |\n| Minifont selection may be unconditional | Should be coverage-driven or policy-driven |\n| Run decorations may be per-cluster | Should be per-run for continuous underline/strike |\n\n---\n\n### Required Termux change\n\nAdd font/coverage/minifont flush conditions to `drawRowText()` / `flushTextRun()`.\n\n```java\n// TerminalRenderer.java \u2014 target flush condition\nboolean fontChanged =\n        mFontFallback.forGrapheme(cluster.text) != currentTypeface;\n\nboolean minifontChanged =\n        Minifont.isMiniFont(cluster.codePoint) != currentMinifontState;\n\nif (styleChanged || fontChanged || minifontChanged) {\n    flushTextRun(...);\n}\n```\n\nAlso ensure decorations are drawn per run, not per individual cluster:\n\n```java\n// TerminalRenderer.java \u2014 target in flushTextRun()\nif (hasUnderline(currentStyle)) {\n    DecorationPainter.drawUnderline(\n        canvas,\n        runX,\n        baseline,\n        runWidth,\n        mCellHeight,\n        resolvedDecoColor,\n        underlineStyle,\n        mTextPaint\n    );\n}\n```\n\n---\n\n## 2.3 Font selection and fallback: Pango vs Android Paint\n\n### VTE\n\nVTE uses PangoCairo:\n\n```text\nfonts-pangocairo.cc\nfonts-pangocairo.hh\n```\n\nImportant concepts:\n\n- `PangoFontDescription`\n- `PangoCoverage`\n- fontconfig fallback chain\n- bold / italic variants\n- metrics-driven cell sizing\n\nLocator:\n\n```bash\nrg -n \"pango_font_get_coverage|pango_coverage_get|PangoFontDescription\" fonts-pangocairo.cc\n```\n\nVTE can ask whether a font covers a codepoint:\n\n```cpp\n// fonts-pangocairo.cc \u2014 simplified\nauto coverage = pango_font_get_coverage(font, language);\n\nif (pango_coverage_get(coverage, c) == PANGO_COVERAGE_EXACT) {\n    use_font();\n} else {\n    try_fallback_or_minifont();\n}\n```\n\n---\n\n### Termux\n\nTermux uses Android `Paint` / `Typeface`.\n\nRelevant files:\n\n```text\nFontFallbackCache.java\nTerminalRenderer.java\n```\n\nLocator:\n\n```bash\nrg -n \"Typeface|hasGlyph|Paint\" FontFallbackCache.java TerminalRenderer.java\n```\n\nAndroid provides:\n\n- `Typeface`\n- `Paint.hasGlyph(String)` on API 23+\n- `Paint.getTextRunAdvances()`\n- `Canvas.drawTextRun()`\n\nBut Android does not provide a full Pango-style itemizer.\n\n---\n\n### Gaps\n\n| Area | VTE | Termux | Needed |\n|---|---|---|---|\n| Coverage query | Pango coverage | `Paint.hasGlyph()` | Use whole grapheme, not single codepoint |\n| Fallback chain | fontconfig | manual chain | Build explicit chain |\n| Emoji font | automatic | implicit or ad hoc | Explicit emoji fallback |\n| Bold/italic variants | Pango description | `Typeface.create()` | Cache variant typefaces |\n| Shaping | Pango layout | `Canvas.drawText()` | Draw whole grapheme/run |\n| Ligature control | Pango attrs | Paint features | Disable terminal-unwanted ligatures |\n\n---\n\n### Required Termux changes\n\nImprove `FontFallbackCache.java`:\n\n```java\n// FontFallbackCache.java \u2014 target\npublic Typeface forGrapheme(String grapheme) {\n    Typeface cached = mGraphemeCache.get(grapheme);\n    if (cached != null) return cached;\n\n    for (Typeface tf : mChain) {\n        mProbePaint.setTypeface(tf);\n\n        if (Build.VERSION.SDK_INT >= 23 && mProbePaint.hasGlyph(grapheme)) {\n            mGraphemeCache.put(grapheme, tf);\n            return tf;\n        }\n    }\n\n    Typeface fallback = mChain[0];\n    mGraphemeCache.put(grapheme, fallback);\n    return fallback;\n}\n```\n\nRecommended chain:\n\n```java\nmChain = new Typeface[] {\n    userFont,\n    Typeface.MONOSPACE,\n    Typeface.create(\"sans-serif\", Typeface.NORMAL),\n    Typeface.create(\"sans-serif-medium\", Typeface.NORMAL),\n    Typeface.create(\"sans-serif-condensed\", Typeface.NORMAL),\n};\n```\n\nDisable unwanted ligatures:\n\n```java\nmTextPaint.setFontFeatureSettings(\"liga 0, clig 0, calt 0\");\nmTextPaint.setLetterSpacing(0f);\n```\n\n---\n\n# 3. B. Special character rendering: Minifont\n\n## 3.1 Coverage comparison\n\nVTE\u2019s minifont is much larger than Termux\u2019s.\n\nVTE:\n\n```text\nminifont.cc\nminifont.hh\nbox-drawing.hh\n```\n\nTermux:\n\n```text\nMinifont.java\nBoxDrawing.java\n```\n\nLocators:\n\n```bash\nrg -n \"0x2500|0x2580|0x25A0|0x2800|0x1FB00\" minifont.cc\nrg -n \"0x2500|0x2580|0x25A0|0x2800|0x1FB00\" Minifont.java BoxDrawing.java\n```\n\n### Coverage table\n\n| Range | Name | VTE | Termux | Gap |\n|---|---|---:|---:|---|\n| `U+2500..U+257F` | Box Drawing | yes | usually yes/partial | alignment/thickness |\n| `U+2580..U+259F` | Block Elements | yes | often partial | missing shades/blocks |\n| `U+25A0..U+25FF` | Geometric Shapes | selected yes | often missing | port selected glyphs |\n| `U+2700..U+27BF` | Dingbats | selected yes | usually missing | optional |\n| `U+2800..U+28FF` | Braille Patterns | yes | usually missing | high priority |\n| `U+1FB00..U+1FBFF` | Symbols for Legacy Computing | yes | usually missing | high priority |\n| supplementary-plane glyphs | various | yes | often missing | must use codepoints |\n\n---\n\n## 3.2 Box drawing grid and thickness\n\n### VTE\n\nVTE uses a 5\u00d75 design grid for many box-drawing glyphs.\n\nReference:\n\n```text\nbox-drawing.hh\n```\n\nLocator:\n\n```bash\nrg -n \"box_drawing|0x2500\" box-drawing.hh\n```\n\nApproximate structure:\n\n```cpp\n// box-drawing.hh \u2014 simplified\nstatic constexpr uint8_t box_drawing_data[128][5] = {\n    // U+2500 etc.\n};\n```\n\nVTE scales the grid to the cell:\n\n```cpp\n// minifont.cc / box drawing rendering \u2014 simplified\nfloat gx = cell_width  / 5.0f;\nfloat gy = cell_height / 5.0f;\n\nfor (int y = 0; y < 5; ++y) {\n    for (int x = 0; x < 5; ++x) {\n        if (bitmap[y] & (1 << x)) {\n            fill_rect(x * gx, y * gy, gx, gy);\n        }\n    }\n}\n```\n\nImportant properties:\n\n- stems align to cell boundaries,\n- light/heavy lines have consistent thickness,\n- junctions do not produce gaps,\n- glyphs are monospace-safe even if the text font has bad box drawing.\n\n---\n\n### Termux\n\nTermux uses:\n\n```text\nBoxDrawing.java\nMinifont.java\n```\n\nLocator:\n\n```bash\nrg -n \"drawBox|BOX|stroke|thickness\" BoxDrawing.java Minifont.java\n```\n\nCommon Termux issues:\n\n1. Uses stroke lines instead of grid rectangles.\n2. Thickness is computed differently.\n3. Heavy/light stems may not match VTE.\n4. Pixel snapping may be missing.\n5. Some glyphs are drawn from ad hoc tables rather than VTE-compatible tables.\n\n---\n\n### Required Termux change\n\nUse VTE-compatible 5\u00d75 grid rendering.\n\n```java\n// BoxDrawing.java \u2014 target\npublic static Path buildPath(int codePoint, int cellW, int cellH) {\n    int index = codePoint - 0x2500;\n    int[] bits = BOX_DRAWING_5X5[index];\n\n    Path path = new Path();\n\n    float gw = cellW / 5f;\n    float gh = cellH / 5f;\n\n    for (int y = 0; y < 5; y++) {\n        int row = bits[y];\n\n        for (int x = 0; x < 5; x++) {\n            if ((row & (1 << x)) != 0) {\n                float left   = x * gw;\n                float top    = y * gh;\n                float right  = left + gw;\n                float bottom = top + gh;\n\n                path.addRect(left, top, right, bottom, Path.Direction.CW);\n            }\n        }\n    }\n\n    return path;\n}\n```\n\nFor thin stems where VTE uses computed stems rather than full grid blocks, use:\n\n```java\nint lightStem = Math.max(1, Math.round(Math.min(cellW, cellH) / 16f));\nint heavyStem = Math.max(lightStem + 1, lightStem * 2);\n```\n\nBut the final visual target should be VTE\u2019s table semantics.\n\n---\n\n## 3.3 Cache key / structure differences\n\n### VTE\n\nVTE\u2019s minifont cache is keyed by more than codepoint.\n\nRelevant files:\n\n```text\nminifont.cc\nminifont.hh\n```\n\nLocator:\n\n```bash\nrg -n \"cache|key|Glyph\" minifont.cc minifont.hh\n```\n\nA VTE-compatible key should include:\n\n```text\ncodepoint\ncell width\ncell height\nscale / DPI\nbold / heavy flag\nstyle variant if any\n```\n\nIf the cached object is a color bitmap, it must also include foreground color.  \nIf the cached object is a path or alpha mask, it should not include foreground color.\n\n---\n\n### Termux\n\nTermux may cache by:\n\n```java\nint codePoint\n```\n\nor by:\n\n```java\ncodePoint + cellWidth + cellHeight\n```\n\nbut may miss:\n\n- bold/heavy flag,\n- scale,\n- foreground color if bitmap is color-baked,\n- style variants.\n\nLocator:\n\n```bash\nrg -n \"LruCache|cache|key\" Minifont.java\n```\n\n---\n\n### Required Termux change\n\nUse a 64-bit cache key.\n\n```java\n// Minifont.java \u2014 target\nprivate static long makeKey(int codePoint, int cellW, int cellH, boolean bold) {\n    return ((long) codePoint << 32) |\n           ((long) cellW << 20) |\n           ((long) cellH << 8) |\n           (bold ? 1L : 0L);\n}\n```\n\nPrefer caching `Path` or alpha masks:\n\n```java\nprivate static final LruCache<Long, Path> sPathCache = new LruCache<>(512);\n\npublic static void drawMiniFont(\n        Canvas canvas,\n        Paint paint,\n        int codePoint,\n        int x,\n        int y,\n        int cellW,\n        int cellH,\n        int fgColor,\n        boolean bold) {\n\n    long key = makeKey(codePoint, cellW, cellH, bold);\n\n    Path path = sPathCache.get(key);\n\n    if (path == null) {\n        path = buildGlyphPath(codePoint, cellW, cellH, bold);\n        sPathCache.put(key, path);\n    }\n\n    int oldColor = paint.getColor();\n    Paint.Style oldStyle = paint.getStyle();\n\n    paint.setColor(fgColor);\n    paint.setStyle(Paint.Style.FILL);\n\n    canvas.save();\n    canvas.translate(x, y);\n    canvas.drawPath(path, paint);\n    canvas.restore();\n\n    paint.setColor(oldColor);\n    paint.setStyle(oldStyle);\n}\n```\n\n---\n\n## 3.4 Braille and legacy computing symbols\n\n### Braille: `U+2800..U+28FF`\n\nVTE supports braille.\n\nLocator:\n\n```bash\nrg -n \"0x2800|braille\" minifont.cc\n```\n\nTermux usually lacks full braille.\n\nBraille dot pattern:\n\n```text\ncp = U+2800 + bits\n\ndot 1: bit 0\ndot 2: bit 1\ndot 3: bit 2\ndot 4: bit 3\ndot 5: bit 4\ndot 6: bit 5\ndot 7: bit 6\ndot 8: bit 7\n```\n\nLayout:\n\n```text\n1 4\n2 5\n3 6\n7 8\n```\n\nTermux target:\n\n```java\nprivate static Path buildBraillePath(int codePoint, int cellW, int cellH) {\n    int dots = codePoint - 0x2800;\n\n    Path path = new Path();\n\n    float dotW = cellW / 3f;\n    float dotH = cellH / 5f;\n\n    float radius = Math.min(dotW, dotH) * 0.25f;\n\n    float[][] positions = {\n        {1f, 1f}, // dot 1\n        {1f, 2f}, // dot 2\n        {1f, 3f}, // dot 3\n        {2f, 1f}, // dot 4\n        {2f, 2f}, // dot 5\n        {2f, 3f}, // dot 6\n        {1f, 4f}, // dot 7\n        {2f, 4f}, // dot 8\n    };\n\n    for (int i = 0; i < 8; i++) {\n        if ((dots & (1 << i)) != 0) {\n            float cx = positions[i][0] * dotW;\n            float cy = positions[i][1] * dotH;\n\n            path.addCircle(cx, cy, radius, Path.Direction.CW);\n        }\n    }\n\n    return path;\n}\n```\n\n---\n\n### Legacy computing: `U+1FB00..U+1FBFF`\n\nVTE supports many Symbols for Legacy Computing.\n\nLocator:\n\n```bash\nrg -n \"0x1FB00|legacy\" minifont.cc\n```\n\nImportant subranges:\n\n| Range | Meaning |\n|---|---|\n| `U+1FB00..U+1FB3B` | block sextants, 2\u00d73 grid |\n| `U+1FB3C..U+1FB6B` | smooth line drawing / diagonal blocks |\n| `U+1FB70..U+1FB87` | block octants, 2\u00d74 / 3\u00d73 style grids |\n| `U+1FB9A..U+1FB9B` | upper/lower half blocks variants |\n| others | legacy terminal graphics |\n\nTermux must:\n\n1. Treat these as supplementary codepoints.\n2. Use `int codePoint`, not `char`.\n3. Add tables or procedural generators.\n4. Cache by codepoint + cell size.\n\nExample supplementary-safe iteration:\n\n```java\nint cp = Character.codePointAt(text, index);\nint charCount = Character.charCount(cp);\n\nif (Minifont.isMiniFont(cp)) {\n    Minifont.drawMiniFont(canvas, paint, cp, x, y, cellW, cellH, fg, bold);\n}\n\nindex += charCount;\n```\n\n---\n\n# 4. C. Emoji rendering\n\n## 4.1 Grapheme segmentation\n\n### VTE\n\nVTE relies on Pango for text shaping and fallback.\n\nRelevant files:\n\n```text\nfonts-pangocairo.cc\ndrawing-cairo.cc\n```\n\nPango handles:\n\n- grapheme-like shaping,\n- combining marks,\n- ZWJ sequences,\n- emoji font fallback,\n- variation selectors,\n- script itemization.\n\nVTE does not need a manual emoji segmenter because the run text is passed to Pango.\n\n---\n\n### Termux\n\nTermux currently uses `RowLayout` clusters.\n\nLocator:\n\n```bash\nrg -n \"Cluster|cluster\" RowLayout.java\n```\n\nIf clusters are built from codepoints or simple surrogate pairs, Termux will mishandle:\n\n```text\n\ud83d\udc68\u200d\ud83d\udc69\u200d\ud83d\udc67\u200d\ud83d\udc66\n\ud83c\udff3\ufe0f\u200d\ud83c\udf08\n\ud83d\udc69\ud83c\udffd\u200d\ud83d\ude80\n\ud83c\uddfa\ud83c\uddf8\n#\ufe0f\u20e3\n```\n\nThese require Unicode grapheme cluster boundaries.\n\n---\n\n### Required Termux change\n\nUse Android ICU `BreakIterator`.\n\n```java\n// RowLayout.java \u2014 target\nimport android.icu.text.BreakIterator;\n\nprivate void buildGraphemeClusters(String rowText, int startColumn) {\n    BreakIterator it = BreakIterator.getCharacterInstance();\n    it.setText(rowText);\n\n    int start = it.first();\n\n    for (int end = it.next();\n         end != BreakIterator.DONE;\n         start = end, end = it.next()) {\n\n        String grapheme = rowText.substring(start, end);\n\n        int width = WcWidth.graphemeWidth(grapheme);\n\n        addCluster(grapheme, width);\n    }\n}\n```\n\nFor older Android API levels, use:\n\n```java\njava.text.BreakIterator\n```\n\nor bundle a small grapheme-cluster implementation.\n\n---\n\n## 4.2 ZWJ sequence handling\n\nA ZWJ sequence is one grapheme cluster but multiple codepoints.\n\nExample:\n\n```text\n\ud83d\udc68\u200d\ud83d\udc69\u200d\ud83d\udc67\u200d\ud83d\udc66\n\nU+1F468 U+200D U+1F469 U+200D U+1F467 U+200D U+1F466\n```\n\nTermux must:\n\n1. Keep the whole sequence in one cluster.\n2. Assign the cluster width 2.\n3. Draw the whole sequence in one `drawText()` / `drawTextRun()` call.\n4. Choose a font that supports the whole sequence.\n\nBad pattern:\n\n```java\nfor (int cp : codePoints) {\n    drawCodepoint(cp);\n}\n```\n\nGood pattern:\n\n```java\nString grapheme = cluster.text;\n\nTypeface tf = mFontFallback.forGrapheme(grapheme);\nmTextPaint.setTypeface(tf);\n\ncanvas.drawText(grapheme, x, baseline, mTextPaint);\n```\n\n---\n\n## 4.3 Variation selectors: VS15 / VS16\n\n### Relevant codepoints\n\n```text\nU+FE0E  VS15  text presentation\nU+FE0F  VS16  emoji presentation\n```\n\n### VTE behavior\n\nVTE\u2019s width table and Pango fallback together handle presentation.\n\nLocator:\n\n```bash\nrg -n \"FE0F|FE0E|variation\" unicode-width.hh minifont.cc fonts-pangocairo.cc\n```\n\nIn general:\n\n- VS15/VS16 are zero-width.\n- VS16 often forces emoji presentation.\n- Emoji presentation often implies width 2 in terminal usage.\n\n---\n\n### Termux requirements\n\nAdd grapheme-level width rules:\n\n```java\n// WcWidth.java \u2014 target\npublic static int graphemeWidth(String grapheme) {\n    int cp = Character.codePointAt(grapheme, 0);\n\n    // Regional indicator pair: flag emoji.\n    if (isRegionalIndicator(cp) &&\n            grapheme.codePointCount(0, grapheme.length()) >= 2) {\n        return 2;\n    }\n\n    // Emoji presentation selector.\n    if (containsVS16(grapheme)) {\n        return 2;\n    }\n\n    int w = width(cp);\n\n    if (w <= 0) {\n        return 1;\n    }\n\n    return w;\n}\n\nprivate static boolean containsVS16(String grapheme) {\n    for (int i = 0; i < grapheme.length(); ) {\n        int cp = Character.codePointAt(grapheme, i);\n\n        if (cp == 0xFE0F) {\n            return true;\n        }\n\n        i += Character.charCount(cp);\n    }\n\n    return false;\n}\n```\n\nSpecial cases:\n\n| Sequence | Expected width |\n|---|---:|\n| `\u263a\ufe0f` | 2 |\n| `\u263a\ufe0e` | 1 or font-dependent |\n| `#\ufe0f\u20e3` | 2 |\n| `\ud83c\uddfa\ud83c\uddf8` | 2 |\n| `\ud83d\udc4d\ud83c\udffd` | 2 |\n| `\ud83d\udc68\u200d\ud83d\udc69\u200d\ud83d\udc67\u200d\ud83d\udc66` | 2 |\n\n---\n\n## 4.4 Width calculation\n\n### VTE\n\nVTE uses a generated two-level table:\n\n```text\nunicode-width.hh\n```\n\nLocator:\n\n```bash\nrg -n \"width_major|width_minor|unichar_width\" unicode-width.hh\n```\n\nApproximate structure:\n\n```cpp\n// unicode-width.hh \u2014 simplified\nstatic constexpr uint8_t width_major[...] = { ... };\nstatic constexpr uint8_t width_minor[][128] = { ... };\n\nint unichar_width(char32_t c) {\n    auto major = width_major[c >> 8];\n    auto minor = width_minor[major][...];\n    return ...;\n}\n```\n\nAdvantages:\n\n- complete Unicode plane coverage,\n- compact,\n- generated from Unicode data,\n- includes emoji and zero-width ranges.\n\n---\n\n### Termux\n\nTermux uses hand-maintained ranges:\n\n```text\nWcWidth.java\n```\n\nLocator:\n\n```bash\nrg -n \"ZERO_WIDTH|WIDE_EASTASIAN|width\" WcWidth.java\n```\n\nTypical structure:\n\n```java\n// WcWidth.java \u2014 simplified\nprivate static final int[][] ZERO_WIDTH = { ... };\nprivate static final int[][] WIDE_EASTASIAN = { ... };\n\npublic static int width(int codePoint) {\n    if (inRanges(codePoint, ZERO_WIDTH)) return 0;\n    if (inRanges(codePoint, WIDE_EASTASIAN)) return 2;\n    return 1;\n}\n```\n\nProblems:\n\n1. Hard to keep current with Unicode.\n2. Missing newer emoji blocks.\n3. Missing grapheme-level rules.\n4. Binary-search ranges are less precise than generated tables.\n\n---\n\n### Missing width entries to check\n\nTermux should explicitly verify these:\n\n```text\nU+1F300..U+1F5FF   Misc Symbols and Pictographs\nU+1F600..U+1F64F   Emoticons\nU+1F680..U+1F6FF   Transport and Map Symbols\nU+1F700..U+1F77F   Alchemical Symbols\nU+1F780..U+1F7FF   Geometric Shapes Extended\nU+1F800..U+1F8FF   Supplemental Arrows-C\nU+1F900..U+1F9FF   Supplemental Symbols and Pictographs\nU+1FA00..U+1FAFF   Symbols and Pictographs Extended-A\nU+2600..U+26FF     Miscellaneous Symbols\nU+2700..U+27BF     Dingbats\nU+1F1E6..U+1F1FF   Regional Indicators\nU+1F3FB..U+1F3FF   Emoji skin tone modifiers\nU+200D             Zero Width Joiner\nU+FE0E             VS15\nU+FE0F             VS16\nU+E0020..U+E007F   Tag characters\nU+E0100..U+E01EF   Variation Selectors Supplement\n```\n\n---\n\n## 4.5 Emoji font fallback\n\n### VTE\n\nPango/fontconfig automatically falls back to an emoji font.\n\nLocator:\n\n```bash\nrg -n \"emoji|fallback|coverage\" fonts-pangocairo.cc\n```\n\n---\n\n### Termux\n\nAndroid usually has Noto Color Emoji, but Termux must not split emoji sequences.\n\n`FontFallbackCache.java` should test the whole grapheme:\n\n```java\n// FontFallbackCache.java \u2014 target\npublic Typeface forGrapheme(String grapheme) {\n    for (Typeface tf : mChain) {\n        mProbePaint.setTypeface(tf);\n\n        if (Build.VERSION.SDK_INT >= 23 && mProbePaint.hasGlyph(grapheme)) {\n            return tf;\n        }\n    }\n\n    return mDefaultTypeface;\n}\n```\n\nDrawing should fit the grapheme into the terminal cluster width:\n\n```java\n// TerminalRenderer.java \u2014 target\nfloat targetWidth = cluster.width * mCellWidth;\nfloat measured = mTextPaint.measureText(cluster.text);\n\nif (measured > 0 && Math.abs(measured - targetWidth) > targetWidth * 0.03f) {\n    mTextPaint.setTextScaleX(targetWidth / measured);\n} else {\n    mTextPaint.setTextScaleX(1f);\n}\n\ncanvas.drawText(cluster.text, x, baseline, mTextPaint);\n\nmTextPaint.setTextScaleX(1f);\n```\n\nUse scaling conservatively. For monospace text, avoid scaling. For emoji, scaling is often acceptable.\n\n---\n\n# 5. D. Cell attribute model\n\n## 5.1 VTE cell attributes\n\nVTE\u2019s cell model is rich.\n\nReference:\n\n```text\ncell.hh\n```\n\nLocator:\n\n```bash\nrg -n \"struct VteCellAttr|underline|overline|hyperlink|shell\" cell.hh\n```\n\nVTE supports approximately:\n\n| Attribute | VTE |\n|---|---:|\n| foreground color | yes |\n| background color | yes |\n| decoration color | yes |\n| bold | yes |\n| italic | yes |\n| blink | yes |\n| dim | yes |\n| invisible | yes |\n| reverse | yes |\n| underline style | yes |\n| strikethrough | yes |\n| overline | yes |\n| protected | yes |\n| boxed / framed | yes in some versions |\n| columns / wide char | yes |\n| fragment | yes |\n| shell integration marks | yes |\n| hyperlink index | yes |\n\nVTE color packing is assisted by:\n\n```text\ncolor-triple.hh\n```\n\nLocator:\n\n```bash\nrg -n \"triple|fore|back|deco\" color-triple.hh\n```\n\n---\n\n## 5.2 Termux cell attributes\n\nTermux uses a compact 64-bit style.\n\nReference:\n\n```text\nTextStyle.java\n```\n\nLocator:\n\n```bash\nrg -n \"encode|decode|EFFECT\" TextStyle.java\n```\n\nLikely supported:\n\n| Attribute | Termux |\n|---|---:|\n| foreground color | yes |\n| background color | yes |\n| bold | yes |\n| italic | yes |\n| underline | usually boolean only |\n| blink | yes |\n| inverse | yes |\n| invisible | yes |\n| strikethrough | yes |\n| dim | maybe |\n| protected | maybe |\n| decoration color | partial / separate array |\n| underline style | missing or partial |\n| overline | missing or partial |\n| boxed | missing |\n| shell integration marks | missing |\n| hyperlink index | missing or partial |\n\n---\n\n## 5.3 Missing attributes in Termux\n\nHigh priority:\n\n1. Underline style:\n   - none\n   - single\n   - double\n   - curly\n\n2. Decoration color:\n   - SGR 58\n\n3. Overline:\n   - SGR 53 / 55\n\nMedium priority:\n\n4. Boxed / framed\n5. Protected attribute correctness\n6. Shell integration marks\n7. Hyperlink index\n\n---\n\n## 5.4 Required Termux extension\n\nOption A: add auxiliary arrays to `TerminalRow.java`.\n\n```java\n// TerminalRow.java \u2014 target\nprivate int[] mDecorationColor;\nprivate int[] mExtendedEffect;\n```\n\nExtended effect packing:\n\n```java\n// TextStyle.java \u2014 target\npublic static final int EXT_UNDERLINE_NONE   = 0;\npublic static final int EXT_UNDERLINE_SINGLE = 1;\npublic static final int EXT_UNDERLINE_DOUBLE = 2;\npublic static final int EXT_UNDERLINE_CURLY  = 3;\n\npublic static final int EXT_OVERLINE   = 1 << 3;\npublic static final int EXT_BOXED      = 1 << 4;\npublic static final int EXT_PROTECTED  = 1 << 5;\npublic static final int EXT_SHELL_MARK = 1 << 6;\n```\n\nOption B: add a second 64-bit style word.\n\n```java\n// TerminalRow.java \u2014 target\nlong[] mStyle;\nlong[] mExtendedStyle;\n```\n\nThis is cleaner but uses more memory.\n\n---\n\n## 5.5 SGR parser changes\n\nVTE parser references:\n\n```text\nsgr.hh\nparser-sgr.hh\n```\n\nLocator:\n\n```bash\nrg -n \"underline|overline|58|53|colon|subparam\" sgr.hh parser-sgr.hh\n```\n\nTermux must support colon subparameters.\n\nRequired SGR sequences:\n\n```text\nSGR 4:0   no underline\nSGR 4:1   single underline\nSGR 4:2   double underline\nSGR 4:3   curly underline\nSGR 21    double underline, where compatible\nSGR 53    overline\nSGR 55    no overline\nSGR 58    underline/decoration color\nSGR 59    reset underline/decoration color\n```\n\nTermux target:\n\n```java\n// TerminalEmulator.java \u2014 SGR handler sketch\ncase 4:\n    if (hasSubParams()) {\n        int sub = getSubParam(0);\n\n        switch (sub) {\n            case 0:\n                setUnderlineStyle(TextStyle.EXT_UNDERLINE_NONE);\n                break;\n            case 1:\n                setUnderlineStyle(TextStyle.EXT_UNDERLINE_SINGLE);\n                break;\n            case 2:\n                setUnderlineStyle(TextStyle.EXT_UNDERLINE_DOUBLE);\n                break;\n            case 3:\n                setUnderlineStyle(TextStyle.EXT_UNDERLINE_CURLY);\n                break;\n        }\n    } else {\n        setUnderlineStyle(TextStyle.EXT_UNDERLINE_SINGLE);\n    }\n    break;\n\ncase 21:\n    setUnderlineStyle(TextStyle.EXT_UNDERLINE_DOUBLE);\n    break;\n\ncase 53:\n    setOverline(true);\n    break;\n\ncase 55:\n    setOverline(false);\n    break;\n\ncase 58:\n    setDecorationColor(parseExtendedColor());\n    break;\n\ncase 59:\n    resetDecorationColor();\n    break;\n```\n\n---\n\n# 6. E. Width table\n\n## 6.1 VTE generated two-level table\n\nVTE uses:\n\n```text\nunicode-width.hh\n```\n\nLocator:\n\n```bash\nrg -n \"width_major|width_minor|unichar_width\" unicode-width.hh\n```\n\nProperties:\n\n- generated,\n- two-level,\n- compact 4-bit values,\n- covers full Unicode range,\n- includes zero-width and wide categories.\n\nConceptual lookup:\n\n```cpp\n// unicode-width.hh \u2014 simplified\nint width = width_minor[width_major[cp >> 8]][cp & 0xFF];\n```\n\n---\n\n## 6.2 Termux hand-maintained table\n\nTermux uses:\n\n```text\nWcWidth.java\n```\n\nLocator:\n\n```bash\nrg -n \"ZERO_WIDTH|WIDE_EASTASIAN\" WcWidth.java\n```\n\nProperties:\n\n- hand-maintained,\n- binary search,\n- easier to inspect,\n- harder to keep complete.\n\n---\n\n## 6.3 Recommended replacement\n\nGenerate Java tables from Unicode data.\n\nSuggested Java structure:\n\n```java\npublic final class WcWidth {\n\n    // major[plane/page]\n    private static final short[] WIDTH_MAJOR = { ... };\n\n    // minor tables, nibble-packed\n    private static final byte[][] WIDTH_MINOR = { ... };\n\n    public static int width(int codePoint) {\n        if (codePoint < 0 || codePoint > 0x10FFFF) {\n            return 1;\n        }\n\n        int major = codePoint >>> 8;\n        int minorIndex = WIDTH_MAJOR[major];\n\n        if (minorIndex < 0) {\n            return 1;\n        }\n\n        byte[] table = WIDTH_MINOR[minorIndex];\n        int minor = codePoint & 0xFF;\n\n        int b = table[minor >> 1] & 0xFF;\n\n        int w;\n\n        if ((minor & 1) == 0) {\n            w = b & 0xF;\n        } else {\n            w = b >>> 4;\n        }\n\n        return w;\n    }\n}\n```\n\nThen add grapheme width:\n\n```java\npublic static int graphemeWidth(String grapheme) {\n    int cp = Character.codePointAt(grapheme, 0);\n\n    if (isRegionalIndicator(cp) &&\n            grapheme.codePointCount(0, grapheme.length()) >= 2) {\n        return 2;\n    }\n\n    if (containsVS16(grapheme)) {\n        return 2;\n    }\n\n    int w = width(cp);\n\n    return w > 0 ? w : 1;\n}\n```\n\n---\n\n# 7. F. IME composition\n\n## 7.1 VTE preedit rendering\n\nVTE draws preedit text as an overlay.\n\nReference:\n\n```text\nwidget.cc\nwidget.hh\n```\n\nLocator:\n\n```bash\nrg -n \"preedit|composition|im_context\" widget.cc widget.hh\n```\n\nVTE features:\n\n- preedit string,\n- preedit cursor position,\n- underline attribute,\n- underline color,\n- preedit rectangle damage.\n\nConceptual VTE rendering:\n\n```cpp\n// widget.cc \u2014 simplified\nif (m_preedit_string) {\n    auto layout = create_pango_layout(m_preedit_string);\n    add_underline_attribute(layout);\n    add_underline_color_attribute(layout);\n    draw_layout_at_cursor_position(layout);\n}\n```\n\n---\n\n## 7.2 Termux current state\n\nTermux handles Android input through:\n\n```text\nTerminalView.java\n```\n\nLocator:\n\n```bash\nrg -n \"InputConnection|composing|setComposingText\" TerminalView.java\n```\n\nAndroid IME composition may be stored in an input connection, but the terminal canvas may not draw the composing text as a terminal overlay.\n\n---\n\n## 7.3 Required Termux changes\n\nAdd composition state:\n\n```java\n// TerminalView.java \u2014 target\nprivate String mComposingText = \"\";\nprivate int mComposingCursor = -1;\nprivate boolean mComposingActive = false;\n```\n\nUpdate from input connection:\n\n```java\n@Override\npublic boolean setComposingText(CharSequence text, int newCursorPosition) {\n    mComposingText = text.toString();\n    mComposingCursor = newCursorPosition;\n    mComposingActive = true;\n\n    invalidate();\n\n    return super.setComposingText(text, newCursorPosition);\n}\n\n@Override\npublic boolean finishComposingText() {\n    mComposingActive = false;\n    mComposingText = \"\";\n    mComposingCursor = -1;\n\n    invalidate();\n\n    return super.finishComposingText();\n}\n```\n\nRender overlay in `TerminalRenderer.java`:\n\n```java\n// TerminalRenderer.java \u2014 target\nif (row == cursorRow && composingActive) {\n    drawComposingText(\n        canvas,\n        composingText,\n        cursorX,\n        top,\n        baseline\n    );\n}\n```\n\nDraw underline:\n\n```java\nprivate void drawComposingText(\n        Canvas canvas,\n        String text,\n        int x,\n        int top,\n        int baseline) {\n\n    mTextPaint.setColor(mComposingTextColor);\n\n    canvas.drawText(text, x, baseline, mTextPaint);\n\n    float width = mTextPaint.measureText(text);\n\n    DecorationPainter.drawUnderline(\n        canvas,\n        x,\n        baseline,\n        width,\n        mCellHeight,\n        mComposingUnderlineColor,\n        TextStyle.EXT_UNDERLINE_SINGLE,\n        mTextPaint\n    );\n}\n```\n\n---\n\n# 8. G. BiDi and complex text\n\n## 8.1 VTE BiDi\n\nVTE has explicit BiDi support.\n\nReference:\n\n```text\nbidi.cc\nbidi.hh\nvterowdata.hh\n```\n\nLocator:\n\n```bash\nrg -n \"BidiRow|BidiMirror|bidi\" bidi.cc bidi.hh vterowdata.hh\n```\n\nVTE features:\n\n- row-level BiDi processing,\n- visual-to-logical mapping,\n- logical-to-visual mapping,\n- character mirroring,\n- caret mapping,\n- selection mapping.\n\n---\n\n## 8.2 Termux current state\n\nTermux may have BiDi helper classes, but they are often not wired into:\n\n```text\nRowLayout.java\nTerminalRenderer.java\nTerminalView.java\n```\n\nLocator:\n\n```bash\nrg -n \"Bidi|bidi|mirror\" RowLayout.java TerminalRenderer.java TerminalView.java\n```\n\n---\n\n## 8.3 What is needed\n\n### Step 1: Detect RTL rows\n\n```java\npublic static boolean hasBidi(char[] text, int len) {\n    for (int i = 0; i < len; i++) {\n        byte direction = Character.getDirectionality(text[i]);\n\n        if (direction == Character.DIRECTIONALITY_RIGHT_TO_LEFT ||\n            direction == Character.DIRECTIONALITY_RIGHT_TO_LEFT_ARABIC) {\n            return true;\n        }\n    }\n\n    return false;\n}\n```\n\n---\n\n### Step 2: Run BiDi algorithm\n\nUse Android ICU if available:\n\n```java\nandroid.icu.text.Bidi bidi = new android.icu.text.Bidi();\nbidi.setPara(text, defaultLevel, null);\n```\n\nOr port VTE\u2019s `BidiRow`.\n\n---\n\n### Step 3: Build visual clusters\n\n`RowLayout.java` must construct clusters in visual order:\n\n```java\nint visualColumn = 0;\n\nfor (int logicalIndex : visualToLogical) {\n    Cluster cluster = buildClusterFromLogicalIndex(logicalIndex);\n    cluster.visualColumn = visualColumn;\n    visualColumn += cluster.width;\n}\n```\n\n---\n\n### Step 4: Mirror characters\n\n```java\nchar mirrored = BidiMirror.mirror(c);\n```\n\nExamples:\n\n```text\n( \u2194 )\n[ \u2194 ]\n{ \u2194 }\n< \u2194 >\n```\n\n---\n\n### Step 5: Map cursor and selection\n\nNeed bidirectional mapping:\n\n```java\nint visualColumn = logicalToVisual(logicalColumn);\nint logicalColumn = visualToLogical(visualColumn);\n```\n\nThis affects:\n\n- cursor drawing,\n- touch selection,\n- mouse reporting,\n- clipboard copy,\n- IME composition.\n\nThis is high effort and should be a later phase.\n\n---\n\n# 9. H. Damage tracking\n\n## 9.1 VTE region-based damage\n\nVTE tracks damaged regions.\n\nReference:\n\n```text\nwidget.cc\nwidget.hh\n```\n\nLocator:\n\n```bash\nrg -n \"damage|invalidate|clip|expose\" widget.cc widget.hh\n```\n\nVTE can invalidate:\n\n- all,\n- row ranges,\n- cell rectangles,\n- cursor old/new positions,\n- preedit area.\n\nConceptual VTE logic:\n\n```cpp\n// widget.cc \u2014 simplified\ninvalidate_cells(start_row, end_row, start_col, end_col);\nqueue_draw_region(damage_rect);\n```\n\nDuring draw:\n\n```cpp\ncairo_rectangle(cr, clip);\ncairo_clip(cr);\ndraw_rows(visible_rows);\n```\n\n---\n\n## 9.2 Termux full redraw\n\nTermux often uses:\n\n```java\ninvalidate();\n```\n\nLocator:\n\n```bash\nrg -n \"invalidate|postInvalidate\" TerminalView.java\n```\n\nThis is simple but expensive.\n\n---\n\n## 9.3 Required Termux changes\n\nAdd dirty-region tracking.\n\n```java\n// TerminalView.java \u2014 target\nprivate final Rect mDirtyRect = new Rect();\nprivate boolean mHasDirty = false;\n\nprivate void damageRect(int left, int top, int right, int bottom) {\n    if (!mHasDirty) {\n        mDirtyRect.set(left, top, right, bottom);\n        mHasDirty = true;\n    } else {\n        mDirtyRect.union(left, top, right, bottom);\n    }\n\n    postInvalidate(mDirtyRect.left, mDirtyRect.top,\n                   mDirtyRect.right, mDirtyRect.bottom);\n}\n\nprivate void damageCell(int row, int col) {\n    int left = col * mRenderer.mCellWidth;\n    int top = row * mRenderer.mCellHeight;\n    int right = left + mRenderer.mCellWidth;\n    int bottom = top + mRenderer.mCellHeight;\n\n    damageRect(left, top, right, bottom);\n}\n\nprivate void damageRow(int row) {\n    int top = row * mRenderer.mCellHeight;\n    int bottom = top + mRenderer.mCellHeight;\n\n    damageRect(0, top, getWidth(), bottom);\n}\n```\n\nIn `onDraw()`:\n\n```java\nRect clip = canvas.getClipBounds();\n\nint firstRow = clip.top / mRenderer.mCellHeight;\nint lastRow = (clip.bottom + mRenderer.mCellHeight - 1) / mRenderer.mCellHeight;\n\nfor (int row = firstRow; row < lastRow; row++) {\n    drawRow(...);\n}\n```\n\nThis requires change notifications from:\n\n```text\nTerminalEmulator.java\nTerminalBuffer.java\nTerminalRow.java\n```\n\nDamage tracking is performance-critical but risky, so it should be introduced after correctness fixes.\n\n---\n\n# 10. I. Prioritized port plan\n\n## Priority matrix\n\n| Priority | Change | Impact | Effort | Main files |\n|---:|---|---:|---:|---|\n| 1 | Align color resolution with VTE | High | Small | `TerminalRenderer.java`, `ColorUtils.java`, `TerminalColors.java` |\n| 2 | Make cursor cluster-aware | High | Small | `TerminalRenderer.java`, `RowLayout.java` |\n| 3 | Replace width table with generated table | High | Medium | `WcWidth.java` |\n| 4 | Add grapheme segmentation for emoji | High | Medium | `RowLayout.java`, `TerminalRenderer.java` |\n| 5 | Improve font fallback for graphemes | High | Medium | `FontFallbackCache.java`, `TerminalRenderer.java` |\n| 6 | Expand minifont coverage | Medium | Medium | `Minifont.java`, `BoxDrawing.java` |\n| 7 | Align box drawing grid/thickness | Medium | Small/Medium | `BoxDrawing.java`, `Minifont.java` |\n| 8 | Add underline styles and decoration colors | Medium | Medium | `TextStyle.java`, `DecorationPainter.java`, `TerminalEmulator.java` |\n| 9 | Extend cell attribute storage | Medium | Medium | `TextStyle.java`, `TerminalRow.java` |\n| 10 | Port OSC color handling | Medium | Small/Medium | `TerminalEmulator.java`, `TerminalColors.java` |\n| 11 | Render IME composition overlay | Medium | Medium | `TerminalView.java`, `TerminalRenderer.java` |\n| 12 | Wire BiDi | High for RTL | Large | `RowLayout.java`, `TerminalView.java`, BiDi helpers |\n| 13 | Damage tracking | Performance | Large | `TerminalView.java`, `TerminalBuffer.java` |\n| 14 | Sixel / image support | Low for text parity | Large | `image.cc` equivalent, buffer/renderer |\n\n---\n\n# 11. Concrete phased implementation plan\n\n## Phase 1: Correct colors and cursor\n\n### 1.1 Port VTE color resolution order\n\nFiles:\n\n```text\nTerminalRenderer.java\nColorUtils.java\nTerminalColors.java\nTerminalColorScheme.java\n```\n\nVTE reference:\n\n```bash\nrg -n \"determine_colors\" drawing-context.cc\nrg -n \"lightness\" color-lightness.cc\n```\n\nTermux target:\n\n```bash\nrg -n \"resolveCellColors\" TerminalRenderer.java\n```\n\nAction:\n\n- implement VTE order,\n- add dimming,\n- add invisible,\n- add bold-bright conditional,\n- add selection/cursor color precedence.\n\nCode: see Section 2.1.\n\nRisk: medium.  \nTest:\n\n```bash\nprintf '\\e[1;31mBold red bright\\e[0m\\n'\nprintf '\\e[2;37mDim white\\e[0m\\n'\nprintf '\\e[7mReverse\\e[0m\\n'\nprintf '\\e[8mInvisible\\e[0m\\n'\nprintf '\\e[1;7;31mBold reverse red\\e[0m\\n'\n```\n\n---\n\n### 1.2 Make cursor cluster-aware\n\nFiles:\n\n```text\nTerminalRenderer.java\nRowLayout.java\n```\n\nVTE reference:\n\n```bash\nrg -n \"cursor\" drawing-context.cc\n```\n\nTermux target:\n\n```bash\nrg -n \"drawRowCursor|cursorWidth\" TerminalRenderer.java\n```\n\nAdd:\n\n```java\n// RowLayout.java\npublic Cluster clusterForColumn(int column) {\n    for (int i = 0; i < clusterCount; i++) {\n        Cluster c = clusters[i];\n\n        if (column >= c.startColumn && column < c.startColumn + c.width) {\n            return c;\n        }\n    }\n\n    return null;\n}\n```\n\nUse:\n\n```java\n// TerminalRenderer.java\nRowLayout.Cluster cluster = layout.clusterForColumn(cursorCol);\n\nint cursorX = cluster.x;\nint cursorW = cluster.width * mCellWidth;\n```\n\nRisk: medium.  \nTest:\n\n```bash\nprintf '\u4e2d\u6587\\n'\nprintf '\ud83d\ude00\\n'\nprintf '\ud83c\uddfa\ud83c\uddf8\\n'\nprintf '\ud83d\udc68\u200d\ud83d\udc69\u200d\ud83d\udc67\u200d\ud83d\udc66\\n'\n```\n\n---\n\n## Phase 2: Correct width and emoji clusters\n\n### 2.1 Replace `WcWidth.java`\n\nFiles:\n\n```text\nWcWidth.java\n```\n\nVTE reference:\n\n```bash\nrg -n \"unichar_width\" unicode-width.hh\n```\n\nAction:\n\n- generate two-level table,\n- add grapheme width rules,\n- handle VS16,\n- handle regional indicators.\n\nCode: see Section 6.3.\n\nRisk: high impact / medium.  \nTest:\n\n```bash\nprintf '\ud83d\ude00\\n'\nprintf '\ud83d\udc4d\ud83c\udffd\\n'\nprintf '\ud83c\uddfa\ud83c\uddf8\\n'\nprintf '\u263a\ufe0f\\n'\nprintf '#\ufe0f\u20e3\\n'\nprintf '\u4e2d\u6587\\n'\n```\n\n---\n\n### 2.2 Add grapheme segmentation to `RowLayout.java`\n\nFiles:\n\n```text\nRowLayout.java\n```\n\nAction:\n\n- use `BreakIterator`,\n- build clusters by grapheme,\n- compute cluster width with `WcWidth.graphemeWidth()`.\n\nCode: see Section 4.1.\n\nRisk: medium/high.  \nReason: changes selection, cursor, copy/paste.\n\n---\n\n### 2.3 Improve `FontFallbackCache.java`\n\nFiles:\n\n```text\nFontFallbackCache.java\nTerminalRenderer.java\n```\n\nAction:\n\n- fallback by whole grapheme,\n- cache results,\n- include emoji font,\n- flush text run on typeface change.\n\nCode: see Section 4.5.\n\nRisk: medium.\n\n---\n\n## Phase 3: Special graphics parity\n\n### 3.1 Expand minifont coverage\n\nFiles:\n\n```text\nMinifont.java\nBoxDrawing.java\n```\n\nVTE reference:\n\n```bash\nrg -n \"0x2500|0x2580|0x25A0|0x2800|0x1FB00\" minifont.cc\n```\n\nAdd:\n\n```text\nU+2580..U+259F\nU+25A0..U+25FF selected\nU+2800..U+28FF\nU+1FB00..U+1FBFF\n```\n\nRisk: medium.\n\n---\n\n### 3.2 Align box drawing with VTE\n\nFiles:\n\n```text\nBoxDrawing.java\nMinifont.java\n```\n\nVTE reference:\n\n```bash\nrg -n \"box_drawing\" box-drawing.hh\n```\n\nAction:\n\n- use 5\u00d75 grid,\n- cache paths,\n- pixel-snap rectangles,\n- match heavy/light stems.\n\nCode: see Section 3.2.\n\nRisk: medium.\n\n---\n\n### 3.3 Fix minifont cache key\n\nFiles:\n\n```text\nMinifont.java\n```\n\nAction:\n\n- key by codepoint + width + height + bold,\n- do not bake foreground color into path cache,\n- if using bitmaps, include color or use alpha masks.\n\nCode: see Section 3.3.\n\nRisk: low.\n\n---\n\n## Phase 4: Attribute and decoration parity\n\n### 4.1 Extend style storage\n\nFiles:\n\n```text\nTextStyle.java\nTerminalRow.java\n```\n\nAdd:\n\n```java\nint[] mDecorationColor;\nint[] mExtendedEffect;\n```\n\nOr:\n\n```java\nlong[] mExtendedStyle;\n```\n\nRisk: medium.\n\n---\n\n### 4.2 Extend SGR parsing\n\nFiles:\n\n```text\nTerminalEmulator.java\n```\n\nVTE reference:\n\n```bash\nrg -n \"underline|overline|58|53\" parser-sgr.hh sgr.hh\n```\n\nAdd:\n\n```text\n4:0\n4:1\n4:2\n4:3\n21\n53\n55\n58\n59\n```\n\nCode: see Section 5.5.\n\nRisk: medium.\n\n---\n\n### 4.3 Improve `DecorationPainter.java`\n\nFiles:\n\n```text\nDecorationPainter.java\n```\n\nAdd:\n\n- single underline,\n- double underline,\n- curly underline,\n- strikethrough,\n- overline,\n- decoration color.\n\nCode:\n\n```java\npublic static void drawUnderline(\n        Canvas canvas,\n        float x,\n        float baseline,\n        float width,\n        int cellHeight,\n        int color,\n        int underlineStyle,\n        Paint paint) {\n\n    if (underlineStyle == TextStyle.EXT_UNDERLINE_NONE) return;\n\n    paint.setColor(color);\n\n    float thickness = Math.max(1f, cellHeight / 16f);\n    float y = baseline + Math.max(1f, cellHeight / 12f);\n\n    switch (underlineStyle) {\n        case TextStyle.EXT_UNDERLINE_SINGLE:\n            canvas.drawRect(x, y, x + width, y + thickness, paint);\n            break;\n\n        case TextStyle.EXT_UNDERLINE_DOUBLE: {\n            float gap = thickness;\n            canvas.drawRect(x, y, x + width, y + thickness, paint);\n            canvas.drawRect(x, y + thickness + gap,\n                            x + width, y + 2 * thickness + gap, paint);\n            break;\n        }\n\n        case TextStyle.EXT_UNDERLINE_CURLY: {\n            Path path = new Path();\n\n            float amplitude = thickness * 2f;\n            float segment = Math.max(4f, cellHeight / 6f);\n\n            path.moveTo(x, y);\n\n            for (float sx = x; sx < x + width; sx += segment) {\n                float cx1 = sx + segment * 0.25f;\n                float cx2 = sx + segment * 0.75f;\n                float ex = Math.min(sx + segment, x + width);\n\n                path.cubicTo(cx1, y - amplitude,\n                             cx2, y + amplitude,\n                             ex, y);\n            }\n\n            Paint.Style oldStyle = paint.getStyle();\n            float oldStroke = paint.getStrokeWidth();\n\n            paint.setStyle(Paint.Style.STROKE);\n            paint.setStrokeWidth(thickness);\n\n            canvas.drawPath(path, paint);\n\n            paint.setStyle(oldStyle);\n            paint.setStrokeWidth(oldStroke);\n            break;\n        }\n    }\n}\n```\n\nRisk: low/medium.\n\n---\n\n## Phase 5: OSC colors\n\n### 5.1 Port OSC color sequences\n\nFiles:\n\n```text\nTerminalEmulator.java\nTerminalColors.java\n```\n\nVTE reference:\n\n```bash\nrg -n \"OSC|10|11|12|17|19\" osc-colors.cc\n```\n\nSupport:\n\n```text\nOSC 4    palette\nOSC 10   default foreground\nOSC 11   default background\nOSC 12   cursor color\nOSC 17   selection background\nOSC 19   selection foreground\nOSC 104  reset palette entries\nOSC 110  reset default foreground\nOSC 111  reset default background\nOSC 112  reset cursor color\nOSC 117  reset selection background\nOSC 119  reset selection foreground\n```\n\nSketch:\n\n```java\ncase 12:\n    setCursorColor(parseColor(spec));\n    break;\n\ncase 17:\n    setSelectionBackground(parseColor(spec));\n    break;\n\ncase 19:\n    setSelectionForeground(parseColor(spec));\n    break;\n```\n\nRisk: medium.\n\n---\n\n## Phase 6: IME overlay\n\nFiles:\n\n```text\nTerminalView.java\nTerminalRenderer.java\nDecorationPainter.java\n```\n\nVTE reference:\n\n```bash\nrg -n \"preedit\" widget.cc\n```\n\nAction:\n\n- store composing text,\n- draw overlay at cursor,\n- underline composing text,\n- draw preedit cursor if provided.\n\nCode: see Section 7.3.\n\nRisk: medium.\n\n---\n\n## Phase 7: BiDi\n\nFiles:\n\n```text\nRowLayout.java\nTerminalView.java\nTerminalRenderer.java\nBiDi helpers\n```\n\nVTE reference:\n\n```bash\nrg -n \"BidiRow|BidiMirror\" bidi.cc bidi.hh\n```\n\nAction:\n\n1. Detect RTL rows.\n2. Run BiDi.\n3. Build visual clusters.\n4. Mirror characters.\n5. Map cursor/selection.\n\nRisk: high.  \nEffort: large.\n\n---\n\n## Phase 8: Damage tracking\n\nFiles:\n\n```text\nTerminalView.java\nTerminalBuffer.java\nTerminalEmulator.java\n```\n\nVTE reference:\n\n```bash\nrg -n \"damage|invalidate\" widget.cc\n```\n\nAction:\n\n- add dirty rectangles,\n- invalidate only changed cells/rows,\n- clip `onDraw()` to damaged region.\n\nCode: see Section 9.3.\n\nRisk: high.  \nEffort: large.\n\n---\n\n# 12. Recommended final ordering\n\nDo this exact order:\n\n```text\n1.  resolveCellColors() VTE order\n2.  ColorUtils lightness/dim\n3.  cursor cluster width\n4.  background run merging\n5.  WcWidth generated table\n6.  grapheme segmentation in RowLayout\n7.  FontFallbackCache grapheme fallback\n8.  emoji width rules: VS16, regional indicators, skin tones\n9.  Minifont cache key fix\n10. Box drawing 5\u00d75 alignment\n11. Braille support\n12. Legacy computing symbols\n13. Underline styles and decoration colors\n14. SGR 4:x, 53/55, 58/59\n15. OSC 10/11/12/17/19\n16. IME composition overlay\n17. BiDi\n18. Damage tracking\n19. Sixel/images\n```\n\n---\n\n# 13. Minimal high-impact patch set\n\nIf you want the fastest visible improvement, implement these five first.\n\n## Patch 1: VTE-compatible color resolution\n\nFiles:\n\n```text\nTerminalRenderer.java\nColorUtils.java\n```\n\nSee Section 2.1.\n\n---\n\n## Patch 2: Cluster-aware cursor\n\nFiles:\n\n```text\nRowLayout.java\nTerminalRenderer.java\n```\n\nSee Section 11, Phase 1.2.\n\n---\n\n## Patch 3: Grapheme segmentation\n\nFiles:\n\n```text\nRowLayout.java\nWcWidth.java\n```\n\nSee Section 4.1 and Section 6.3.\n\n---\n\n## Patch 4: Grapheme-aware font fallback\n\nFiles:\n\n```text\nFontFallbackCache.java\nTerminalRenderer.java\n```\n\nSee Section 4.5.\n\n---\n\n## Patch 5: Minifont cache and box drawing alignment\n\nFiles:\n\n```text\nMinifont.java\nBoxDrawing.java\n```\n\nSee Section 3.2 and Section 3.3.\n\n---\n\n# 14. Summary of biggest architectural differences\n\n| Subsystem | VTE | Termux | Main port action |\n|---|---|---|---|\n| Color | centralized `determine_colors()` | `resolveCellColors()` | port VTE order |\n| Dimming | perceived lightness | often missing/simple | port `color-lightness.cc` |\n| Runs | Pango text runs | clusters | flush on font/coverage/grapheme |\n| Font fallback | Pango coverage | Android `Paint` | grapheme-based fallback cache |\n| Emoji | Pango fallback/shaping | cluster scaling | grapheme segmentation + width rules |\n| Width | generated two-level table | hand-maintained ranges | generate table |\n| Minifont | very large coverage | smaller | add braille/legacy/block coverage |\n| Box drawing | 5\u00d75 grid | ad hoc | align grid/thickness |\n| Attributes | rich cell attr | compact long | extended style arrays |\n| SGR | colon subparams | often basic | parse `4:x`, `58`, `53` |\n| IME | preedit overlay | input connection only | draw composition overlay |\n| BiDi | integrated | unused/partial | wire visual reordering |\n| Damage | region-based | full invalidate | dirty rectangles |\n\nThe single most important correctness upgrade is:\n\n```text\nVTE-compatible color resolution + generated width table + grapheme-cluster emoji rendering\n```\n\nThe single most important special-character upgrade is:\n\n```text\nVTE-compatible minifont coverage + 5\u00d75 box drawing alignment + correct cache key\n```\n\nThe single most important long-term architectural upgrade is:\n\n```text\nBiDi-aware RowLayout + damage tracking\n```", "session_id": "796ec1d9-bb3b-4454-879f-89a2605e5a9f", "last_msg_id": "8b55638c-27c0-4c1f-8af1-12e8c912f6b1"}